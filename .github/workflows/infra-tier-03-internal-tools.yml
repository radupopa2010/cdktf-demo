name: tier-03-internal-tools

on:
  workflow_dispatch:
    inputs:
      apply:
        required: false
        type: boolean
        default: true
  push:
    branches: [main]
    paths:
      - "tier-03-cdktf-internal-tools/**"
      - ".github/workflows/infra-tier-03-internal-tools.yml"
      - ".github/workflows/_infra-shared-cdktf-tier.yml"
  workflow_call:
    inputs:
      apply:
        required: false
        type: boolean
        default: true

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/_infra-shared-cdktf-tier.yml
    with:
      tier_dir: tier-03-cdktf-internal-tools
      stack: devnet
      apply: ${{ inputs.apply == null && true || inputs.apply }}
