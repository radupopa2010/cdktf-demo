apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-demo.fullname" . }}
  labels:
    {{- include "rust-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rust-demo.selectorLabels" . | nindent 4 }}
